%h1 Listing mappings

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @mappings.each do |mapping|
      %tr
        %td= link_to 'Show', mapping
        %td= link_to 'Edit', edit_mapping_path(mapping)
        %td= link_to 'Destroy', mapping, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Mapping', new_mapping_path
